[package]
name = "ibc-support"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# ibc crates
ibc = { version = "0.21.0", default-features = false,git = "https://github.com/octopus-network/ibc-rs.git", branch = "dev"}
ibc-proto = { version = "0.21.0", default-features = false, git = "https://github.com/octopus-network/ibc-proto-rs.git", branch = "dev"}

# substrate crates
codec = { package = "parity-scale-codec", version = "3.0.0", default-features = false, features = ["derive"] }
scale-info = { version = "2.1.2", default-features = false, features = ["derive"] }
sp-runtime = { git = "https://github.com/paritytech/substrate.git", branch = "polkadot-v0.9.30", default-features = false }


[features]
default = ['std']
std = [
    "ibc/std",
    "ibc-proto/std",
    "codec/std",
    "scale-info/std",
    "sp-runtime/std",
]
